% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/with-bones.R
\name{withBones}
\alias{withBones}
\title{Wrap a Shiny output in a skeleton placeholder}
\usage{
withBones(
  ui,
  type = NULL,
  rows = 6L,
  cols = 4L,
  lines = 3L,
  n = 3L,
  height = NULL,
  animation = NULL,
  stale = NULL,
  delay = NULL,
  min_time = NULL,
  remember = NULL
)
}
\arguments{
\item{ui}{A Shiny output, such as \code{plotOutput("chart")}.}

\item{type}{The shape of the skeleton: \code{"text"}, \code{"table"}, \code{"plot"},
\code{"cards"}, \code{"value"}, a chart shape (\code{"bar"}, \code{"line"}, \code{"scatter"},
\code{"area"}, \code{"histogram"}, \code{"pie"}, \code{"heatmap"}, \code{"map"}, \code{"network"},
\code{"timeline"}, \code{"wordcloud"}), or a table package shape (\code{"dt"},
\code{"reactable"}, \code{"gt"}, \code{"rhandsontable"}). \code{NULL} (the default) gets the
shape from \code{ui}.}

\item{rows, cols}{The number of body rows and columns, when \code{type} is
\code{"table"} or a table package shape.}

\item{lines}{The number of lines, when \code{type} is \code{"text"}.}

\item{n}{The number of cards or values, when \code{type} is \code{"cards"} or
\code{"value"}.}

\item{height}{A CSS height for the placeholder, or a number of pixels, as
in Shiny. \code{NULL} uses the height of the output. If the output has no
height, \code{NULL} uses a default for the type.}

\item{animation}{\code{"wave"}, \code{"pulse"} or \code{"none"}. \code{NULL} uses the value
from \code{\link[=bones_defaults]{bones_defaults()}}.}

\item{stale}{\code{TRUE} keeps the old content on the screen, dimmed, while
the output calculates again. \code{FALSE} shows the skeleton again. \code{NULL}
uses the value from \code{\link[=bones_defaults]{bones_defaults()}}, which is \code{TRUE} if you did not
set it.}

\item{delay}{The time in milliseconds before the skeleton, or the
dimming, appears. A load that ends sooner shows nothing, so a fast
output does not flicker. The space of the content is kept from the
start. \code{NULL} uses the value from \code{\link[=bones_defaults]{bones_defaults()}}, which is 300 if
you did not set it.}

\item{min_time}{The shortest time in milliseconds that the skeleton, or
the dimming, stays on the screen once it has appeared. A load that ends
a moment after \code{delay} thus does not flash a skeleton for one frame.
\code{NULL} uses the value from \code{\link[=bones_defaults]{bones_defaults()}}, which is 500 if you did
not set it.}

\item{remember}{\code{TRUE} stores the real height of the content in the
browser, and keeps that height on the next visit in place of the
estimate, so the page does not move when the content arrives. Only a
height from an estimate is stored: a height from \code{height}, or from the
output itself, is exact already. The height is kept per page and per
output id, and it is used only when the output has almost the same
width as when it was measured. For a plotly, echarts4r or highcharter
output, \code{remember} also stores the kind of the chart (see "Shape"). \code{NULL} uses the value from
\code{\link[=bones_defaults]{bones_defaults()}}, which is \code{TRUE} if you did not set it.}
}
\value{
\code{ui}, in a placeholder container.
}
\description{
Shows a grey placeholder in the shape of the content while the output
loads for the first time. When the output calculates again, the old
content stays on the screen and is dimmed. The user is possibly reading
that content, and grey boxes in its place would remove information and
add none.
}
\section{Shape}{
The shape comes from the output that you give: \code{plotOutput()} gets the
shape of a chart, \code{tableOutput()} gets rows and columns, and
\code{textOutput()} gets lines of text. Use \code{type} to set a different shape.
You must do this for \code{uiOutput()}, because its content is not known
before it arrives.

A \code{plotOutput()} gets the shape of a bar chart. If the chart is of
another kind, give that kind: \code{"line"}, \code{"scatter"}, \code{"area"},
\code{"histogram"}, \code{"pie"} or \code{"heatmap"}. The output cannot tell, because
\code{renderPlot()} decides the kind later, on the server.

A table from a table package gets the shape of that package, found from
its output: \code{DT::DTOutput()} gets \code{"dt"}, \code{reactable::reactableOutput()}
gets \code{"reactable"}, \code{gt::gt_output()} gets \code{"gt"}, and
\code{rhandsontable::rHandsontableOutput()} gets \code{"rhandsontable"}. Each shape
copies the parts of its package, such as the search box of DT. Set \code{rows}
to the number of rows on a page: DT and reactable show 10 by default.

A visualisation widget gets the shape of its kind, found from its output
in the same way. A map (leaflet, tmap, mapview, mapdeck and others) gets
\code{"map"}. A network or a diagram (visNetwork, DiagrammeR, networkD3,
collapsibleTree) gets \code{"network"}. timevis gets \code{"timeline"}, wordcloud2
gets \code{"wordcloud"}, and dygraphs gets \code{"line"}. ggmap and a ggplot2 map
draw into a \code{plotOutput()}, so give them \code{type = "map"}.

A plotly, echarts4r or highcharter output can draw any kind of chart. With
no \code{type}, it finds its kind by itself, but only when its first value
arrives in the browser: the spec of the chart names the kind of each
series. The first skeleton thus has the bar shape. The next loads, with
\code{stale = FALSE}, show the shape of the kind: \code{"bar"}, \code{"line"}, \code{"area"},
\code{"scatter"}, \code{"histogram"}, \code{"pie"}, \code{"heatmap"}, \code{"map"}, \code{"network"} or
\code{"wordcloud"}. With \code{remember}, the kind is also stored in the browser, so
on the next visit the first skeleton has that shape too. The first series
that names a kind decides. A chart of a kind with no shape, such as a box
plot, keeps the bar shape. A \code{type} that you give always wins. Other
widgets that can draw any chart, such as ggiraph, keep the bar shape: give
their kind with \code{type}.
}

\section{Layout}{
The wrapper keeps the space of the output until the content arrives, so
the page does not move. If the output sets its own height, as
\code{plotOutput()} does, the wrapper uses that height. If not, the height
comes from \code{rows}, \code{lines} or \code{n}, or you can set it with \code{height}. When
the content arrives, the content sets the height.
}

\examples{
if (requireNamespace("shiny", quietly = TRUE)) {
  library(shiny)

  withBones(plotOutput("chart"))
  withBones(tableOutput("results"), rows = 8, cols = 5)
  withBones(uiOutput("cards"), type = "cards", n = 4)
  withBones(plotOutput("trend"), type = "line")
}
}
