% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auto.R
\name{bones_auto}
\alias{bones_auto}
\title{Wrap every Shiny output in a UI in a skeleton placeholder}
\usage{
bones_auto(ui, ..., exclude = NULL)
}
\arguments{
\item{ui}{A UI: a page, a tag, or a tag list.}

\item{...}{Arguments for \code{\link[=withBones]{withBones()}}, used for every output that is
wrapped: for example \code{stale = FALSE} or \code{animation = "pulse"}. Not
\code{type}, because each output gets the shape of its own kind.}

\item{exclude}{The ids of outputs to leave alone.}
}
\value{
\code{ui}, with each output in a placeholder container. A page keeps
its class and its theme.
}
\description{
Gives skeletons to an app with no change to each output. \code{bones_auto()}
looks through a UI and puts each Shiny output in \code{\link[=withBones]{withBones()}}. Each one
gets the shape of its output, as with \code{withBones(output)}: a
\code{plotOutput()} gets a chart, a \code{tableOutput()} gets rows, a DT table
gets the shape of DT, and so on.
}
\details{
Give it the whole page, or only a part of it:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{ui <- bones_auto(page_sidebar(...))
ui <- page_sidebar(sidebar = ..., bones_auto(navset_card_tab(...)))
}\if{html}{\out{</div>}}
}
\section{What it leaves alone}{
\itemize{
\item An output that is already in \code{\link[=withBones]{withBones()}}. To give one output its own
shape or options, wrap it yourself: \code{bones_auto()} keeps that wrapper.
\item An output whose id is in \code{exclude}.
\item An inline output, such as \code{textOutput("n", inline = TRUE)}. The
wrapper is a block, so it would break the line of text.
}

A \code{uiOutput()} can hold anything, so it gets the text shape. Wrap it
yourself to give it another shape, such as \code{type = "cards"}.

\code{bones_auto()} sees only the UI that you give it. Outputs that a
\code{renderUI()} makes later are not in it. Call \code{bones_auto()} on the UI
inside that \code{renderUI()} to give them skeletons too.
}

\examples{
if (requireNamespace("shiny", quietly = TRUE)) {
  library(shiny)

  ui <- bones_auto(fluidPage(
    plotOutput("chart"),
    tableOutput("results"),
    textOutput("count", inline = TRUE),         # inline: left alone
    withBones(uiOutput("cards"), type = "cards") # wrapped already: kept
  ))
}
}
