% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/block.R
\name{bones_block}
\alias{bones_block}
\title{A grey block to build your own placeholder}
\usage{
bones_block(width = "100\%", height = "0.75rem", shape = c("rect", "circle"))
}
\arguments{
\item{width}{A CSS width, or a number of pixels. \code{"100\%"} fills the width.}

\item{height}{A CSS height, or a number of pixels. The default is the
height of one line of text in the built-in shapes.}

\item{shape}{\code{"rect"} for a block with the corner radius of bones, or
\code{"circle"} for a round block, such as an avatar. Give a circle the same
width and height.}
}
\value{
An \link[htmltools:tag]{htmltools::tag}.
}
\description{
The building block of a placeholder that you make yourself. Give the
blocks to \code{\link[=withBones]{withBones()}} as \code{skeleton}, laid out with any tags. Each block
takes the colours, the corner radius and the animation of bones, so your
placeholder moves and looks like the built-in shapes.
}
\details{
The tags at the top level of \code{skeleton} stack with a small gap. Inside a
tag of your own, you set the layout: for example \code{display: flex} for a
row, or \verb{display: grid; gap: 0.5rem} for a column.
}
\examples{
# A line of text, 60\% wide.
bones_block("60\%")

# An avatar.
bones_block(48, 48, shape = "circle")

if (requireNamespace("shiny", quietly = TRUE)) {
  library(shiny)

  # A list of three people: an avatar, a name and a line of text each.
  person <- div(
    style = "display: flex; gap: 0.75rem; align-items: center;",
    bones_block(40, 40, shape = "circle"),
    div(
      style = "flex: 1; display: grid; gap: 0.4rem;",
      bones_block("40\%"), bones_block("80\%", "0.6rem")
    )
  )
  withBones(uiOutput("people"), skeleton = tagList(person, person, person))
}
}
\seealso{
\code{\link[=withBones]{withBones()}} and its section "Your own placeholder".
}
